__author__ = 'Антон Брагин'

alphabet = {'a', 'b', 'c', 'd'}

max_mask = 2 ** len(alphabet) - 1

print('Max: {0:b}'.format(max_mask))

mask = max_mask

print('Mask: {0:b}'.format(mask))

while mask > 0:
    pass
